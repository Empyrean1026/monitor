import { Router } from 'express'; import { customerListController,customerSummaryController,highValueCustomersController } from '../controllers/customer.controller.js'; import { authMiddleware } from '../middlewares/auth.js'; import { asyncHandler } from '../utils/async-handler.js'; import { validate } from '../middlewares/validate.js'; import { customerListQuerySchema } from '../validators/customer.validator.js';
export const customerRouter=Router(); customerRouter.get('/summary',authMiddleware,asyncHandler(customerSummaryController)); customerRouter.get('/high-value',authMiddleware,asyncHandler(highValueCustomersController)); customerRouter.get('/',authMiddleware,validate(customerListQuerySchema,'query'),asyncHandler(customerListController));
